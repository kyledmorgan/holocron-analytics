# Security and Secrets Policy

- Never commit credentials, tokens, or connection strings.
- Use `.env.example` and `config/*.example.yaml` for templates only.
- Keep `local/` and `.env` ignored by git.
