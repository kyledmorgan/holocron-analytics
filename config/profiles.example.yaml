# Example profiles configuration (template only)
# Do not store secrets here

profiles:
  local:
    db:
      engine: "sqlserver"
      host: "localhost"
      port: 1434
      database: "Holocron"
      user: "REPLACE_ME"
      password: "REPLACE_ME"
