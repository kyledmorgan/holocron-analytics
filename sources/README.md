# Sources

This folder defines source metadata and mapping templates only. It does not store payloads, scraped text, or credentials.

- `catalog/`: Example source definitions (no secrets).
- `mappings/`: Example mapping templates.
